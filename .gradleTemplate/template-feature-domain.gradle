apply plugin: 'com.android.library'
apply from: "$rootDir/android_base.gradle"

android {
    namespace "${project.applicationId}.features.{{featureName}}.{{featureLayer}}"
}

dependencies {
    implementation libs.hiltAndroid
}