apply from: "$rootDir/android_feature_ui.gradle"

android {
    namespace "${AndroidConfig.applicationId}.features.repository.details.ui"
}

dependencies {
    implementation projects.features.featureRepository.domain
    implementation Libraries.glideAndroid
}
