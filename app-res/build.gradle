apply plugin: 'com.android.library'
apply from: "$rootDir/android_base.gradle"

android {
    namespace "${AndroidConfig.applicationId}.app.res"
}

dependencies {
    implementation Libraries.material
}
