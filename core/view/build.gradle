apply plugin: 'com.android.library'
apply from: "$rootDir/android_base.gradle"

android {
    namespace "${project.applicationId}.core.view"
}

dependencies {
    implementation libs.androidxConstraintLayout
}